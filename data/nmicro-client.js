// configuration NMICRO-CLIENT
module.exports = {
	// // Log folder mode "test" default: ./log
	// logFolder: '/path/to/log',
	// language lists
	// default language is the first in the list
	langues: ['fr', 'en'],
	// only recompiled model when changing file
	defaultModel: 'default',
	// only recompiled mode when changing files (web or app)
	defaultMode: 'web',
	// server IP
	serverIP: '127.0.0.1',
	// server port
	serverPort: 8080
};
